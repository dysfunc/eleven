const { concat, each, filter, forEach, includes, indexOf, pop, push, reduce, slice, splice, reverse, shift, unshift } = [];

export { concat, each, filter, forEach, includes, indexOf, pop, push, reduce, slice, splice, reverse, shift, unshift };
