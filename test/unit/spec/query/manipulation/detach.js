import Eleven from '../../../../../src/eleven';
const $ = Eleven.query;

describe('DOM element removal methods', () => {
  describe('$.fn.remove', () => {
    // it('should remove an element, its data and event bindings from the DOM', () => {
    //   $('body').append('<div id="test"></div>');
    //
    //   var element = $('#test');
    //
    //   element.remove();
    //   expect($('#test').length).toEqual(0);
    // });
  });
});
